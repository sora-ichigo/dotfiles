name: Provisioning Test
on: 
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: setup mitamae
        run: ./bin/setup
      - name: mitamae local
        run: make mitamae-dry
